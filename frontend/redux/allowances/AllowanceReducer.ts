import { createSlice } from "@reduxjs/toolkit";

const allowanceSlice = createSlice({
  name: "allowance",
  initialState: {},
  reducers: {},
});

export const {} = allowanceSlice.actions;
export default allowanceSlice.reducer;
